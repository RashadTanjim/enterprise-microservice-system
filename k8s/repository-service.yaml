apiVersion: v1
kind: Service
metadata:
  name: repository-service
  namespace: enterprise-ms
  labels:
    app.kubernetes.io/name: repository-service
spec:
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app.kubernetes.io/name: repository-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: repository-service
  namespace: enterprise-ms
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: repository-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: repository-service
    spec:
      containers:
        - name: repository-service
          image: ghcr.io/your-org/enterprise-microservice-system/repository-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: REPOSITORY_SERVICE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_PASSWORD
            - name: AUTH_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: AUTH_JWT_SECRET
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_PASSWORD
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 20
